http:
  port: "8891"

scheduler:
  cpuIntensive:
    size: "2"

clouderby:
  host: "localhost"
  port: "8081"
  user: "mule"
  password: "${CLOUDERBY_PASSWORD}"

oracle:
  host: "cloud-services.demos.mulesoft.com"
  port: "32627"
  user: "mule"
  password: "${ORACLE_PASSWORD}"
  serviceId: "muledb"

